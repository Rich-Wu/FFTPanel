<% @users.each do |user| %>
  <div class = "menu">
    <%= user.first_name %><br>
    <%= image_tag("/assets/portraits/#{user.avatar}") %>
    <%= link_to "show", user_path(user.id), class: "navitem" %>
  </div>
<% end %>
